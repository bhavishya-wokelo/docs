---
title: "Fundraising Activity"
hidden: true
---

Surface fundraising activity across a topic, industry, or market segment. Given a topic (e.g. `"organic food delivery"`, `"climate tech"`, `"generative AI"`), the API returns a list of funding rounds raised in that space — with structured details on the company, round, investors, and geography.

NOTE: This is an asynchronous API. It returns a `report_id`, which you can use to fetch the [report status](https://docs.wokelo.ai/supporting-apis-doc#report-status) and later call the [download report](https://docs.wokelo.ai/supporting-apis-doc#download-report) endpoint to retrieve the final report.

## Request reference

Here is a sample request containing all parameters:

<CodeGroup>

```bash cURL expandable wrap
curl --location 'https://api.wokelo.ai/api/assets/process_common/' \
--header 'Authorization: Bearer Token' \
--header 'Content-Type: application/json' \
--data '{
  "reportType": "fundraisers_workflow",
  "payload": {
    "topic": "organic food delivery",
    "workbook_name": "organic food delivery",
    "advanced_settings": {
      "company_type": "all",
      "geography": [
        "Worldwide"
      ],
      "funding_stage": [],
      "employee_count": [],
      "total_funding_include_blanks": true
    }
  },
  "reportSource": "WKLAPP"
}'
```

```python Python expandable wrap
import requests
import json

url = "https://api.wokelo.ai/api/assets/process_common/"

payload = json.dumps({
  "reportType": "fundraisers_workflow",
  "payload": {
    "topic": "Organic food delivery",
    "workbook_name": "Organic food delivery",
    "advanced_settings": {
      "company_type": "all",
      "geography": [
        "Worldwide"
      ],
      "funding_stage": [],
      "employee_count": [],
      "total_funding_include_blanks": True
    }
  },
  "reportSource": "WKLAPP"
})

headers = {
  "Authorization": "Bearer Token",
  "Content-Type": "application/json"
}

response = requests.post(url, headers=headers, data=payload)

print(response.text)
```

</CodeGroup>

The `topic` field specifies the industry, segment, or product category for which fundraising rounds should be returned. Use `parameters` to filter the deal universe.

Use `advanced_settings` to filter the fundraising universe by:

| Parameter | Description |
| --- | --- |
| `company_type` | Filter by company type (`private`, `public`, or `all`) |
| `geography` | Geographic scope as [ISO country codes](https://docs.wokelo.ai/iso-country-codes) (e.g. `["USA", "GBR"]`) |
| `funding_stage` | Filter by funding stage |
| `employee_count` | Filter by employee size buckets |
| `total_funding_include_blanks` | Include companies with missing funding data |

Pass empty arrays (`[]`) to include all available options.

## Examples

### Fundraising Workflow

Get all the funding activities in the EdTech domain.

#### Sample Request

<CodeGroup>

```bash cURL expandable wrap
curl --location 'https://api.wokelo.ai/api/assets/process_common/' \
--header 'Authorization: Bearer Token' \
--header 'Content-Type: application/json' \
--data '{
  "reportType": "fundraisers_workflow",
  "payload": {
    "topic": "EdTech",
    "workbook_name": "EdTech",
    "advanced_settings": {
      "company_type": "all",
      "geography": [
        "Worldwide"
      ],
      "funding_stage": [],
      "employee_count": [],
      "total_funding_include_blanks": true
    }
  },
  "reportSource": "WKLAPP"
}'
```

```python Python expandable wrap
import requests
import json

url = "https://api.wokelo.ai/api/assets/process_common/"

payload = json.dumps({
  "reportType": "fundraisers_workflow",
  "payload": {
    "topic": "EdTech",
    "workbook_name": "EdTech",
    "advanced_settings": {
      "company_type": "all",
      "geography": [
        "Worldwide"
      ],
      "funding_stage": [],
      "employee_count": [],
      "total_funding_include_blanks": True
    }
  },
  "reportSource": "WKLAPP"
})

headers = {
  "Authorization": "Bearer Token",
  "Content-Type": "application/json"
}

response = requests.post(url, headers=headers, data=payload)

print(response.text)
```

</CodeGroup>

#### Sample Response

```json expandable wrap
{
  "report_id": 123345
}
```